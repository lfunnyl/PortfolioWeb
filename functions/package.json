{
  "name": "portfolio-functions",
  "description": "PortfolioWeb Firebase Cloud Functions — Fiyat, Haber, Teknik Analiz",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.5",
    "node-fetch": "^3.3.2",
    "yahoo-finance2": "^2.11.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/node": "^25.9.1",
    "typescript": "^5.0.0"
  },
  "private": true
}
